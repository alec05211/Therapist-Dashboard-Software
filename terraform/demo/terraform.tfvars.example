# Replace with the public IP shown by a trusted "what is my IP" service, with /32.
demo_allowed_cidr = "203.0.113.10/32"

# Keep mock artifacts only as long as needed for the demo.
artifact_retention_days = 30
